<% if (title) { %>
  <h1>
    <%= title %>
  </h1>
  <% } %>

    <%- messages('notice') %>
      <%- messages('error') %>

        <% if (errors) { %>
          <ul class="notice">
            <% errors.array().forEach(error=> { %>
              <li>
                <%= error.msg %>
              </li>
              <% }) %>
          </ul>
          <% } %>

            <!-- Account Info Update Form -->
            <form action="/account/update" method="post" class="form-container">
              <h2>Update Account Information</h2>

              <label for="account_firstname">First Name</label>
              <input type="text" id="account_firstname" name="account_firstname" required
                value="<%= typeof account_firstname !== 'undefined' ? account_firstname : '' %>">

              <label for="account_lastname">Last Name</label>
              <input type="text" id="account_lastname" name="account_lastname" required
                value="<%= typeof account_lastname !== 'undefined' ? account_lastname : '' %>">

              <label for="account_email">Email</label>
              <input type="email" id="account_email" name="account_email" required
                value="<%= typeof account_email !== 'undefined' ? account_email : '' %>">

              <input type="hidden" name="account_id" value="<%= typeof account_id !== 'undefined' ? account_id : '' %>">
              <button type="submit">Update Account</button>
            </form>

            <!-- Password Update Form -->
            <form action="/account/update-password" method="post" class="form-container">
              <h2>Change Password</h2>

              <label for="account_password">New Password</label>
              <input type="password" id="account_password" name="account_password" required>

              <p class="password-rules">
                Passwords must be at least 12 characters and include 1 capital letter, 1 number, and 1 special
                character.
              </p>

              <input type="hidden" name="account_id" value="<%= typeof account_id !== 'undefined' ? account_id : '' %>">
              <button type="submit">Change Password</button>
            </form>